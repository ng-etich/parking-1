<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      color: #333;
      overflow-x: hidden;
    }

    /* Header Styles */
    header {
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(10px);
      color: white;
      padding: 1rem 2rem;
      box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
      border-bottom: 1px solid rgba(255, 255, 255, 0.2);
    }

    .header-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
      max-width: 1200px;
      margin: 0 auto;
    }

    .logo {
      font-size: 1.8rem;
      font-weight: 800;
      background: linear-gradient(45deg, #ffd89b, #19547b);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .nav-links {
      display: flex;
      gap: 1.5rem;
    }

    .nav-links a {
      color: white;
      text-decoration: none;
      font-weight: 500;
      transition: all 0.3s;
      padding: 0.5rem 1rem;
      border-radius: 20px;
    }

    .nav-links a:hover, .nav-links a.active {
      background: rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(10px);
    }

    /* Main Container */
    .container {
      max-width: 1200px;
      margin: 2rem auto;
      padding: 0 1.5rem;
    }

    /* Page Title */
    h2 {
      color: white;
      margin-bottom: 1.5rem;
      padding-bottom: 0.5rem;
      font-weight: 700;
      font-size: 2.5rem;
      text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
      text-align: center;
    }

    /* Alert Messages */
    .alert {
      padding: 1rem;
      border-radius: 10px;
      margin-bottom: 1.5rem;
      font-weight: 500;
    }

    .alert-success {
      background: rgba(72, 187, 120, 0.2);
      color: #2d3748;
      border: 1px solid rgba(72, 187, 120, 0.3);
    }

    .alert-error {
      background: rgba(245, 101, 101, 0.2);
      color: #2d3748;
      border: 1px solid rgba(245, 101, 101, 0.3);
    }

    /* Stats Section */
    .stats-section {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      padding: 3rem 2rem;
      border-radius: 20px;
      margin: 2rem 0;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.5);
    }

    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 2rem;
      max-width: 1200px;
      margin: 0 auto;
    }

    .stat-item {
      text-align: center;
      padding: 1.5rem;
      transition: all 0.3s ease;
      cursor: pointer;
    }

    .stat-item:hover {
      transform: translateY(-5px);
    }

    .stat-number {
      font-size: 2.5rem;
      font-weight: 800;
      margin-bottom: 0.5rem;
      background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .stat-label {
      font-size: 1rem;
      color: #4a5568;
      font-weight: 500;
    }

    /* Dashboard Grid */
    .dashboard-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
      gap: 2rem;
      margin-top: 2rem;
    }

    .dashboard-card {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      border-radius: 20px;
      padding: 2rem;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.5);
      transition: all 0.3s ease;
    }

    .dashboard-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 50px rgba(0, 0, 0, 0.2);
    }

    .card-header {
      display: flex;
      justify-content: between;
      align-items: center;
      margin-bottom: 1.5rem;
      padding-bottom: 1rem;
      border-bottom: 2px solid #e2e8f0;
    }

    .card-title {
      font-size: 1.3rem;
      font-weight: 700;
      color: #2c3e50;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .card-actions {
      display: flex;
      gap: 0.5rem;
    }

    .btn {
      padding: 0.75rem 1.5rem;
      border: none;
      border-radius: 50px;
      font-size: 0.9rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      text-decoration: none;
    }

    .btn-primary {
      background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
      color: white;
      box-shadow: 0 4px 15px rgba(106, 17, 203, 0.3);
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(106, 17, 203, 0.4);
    }

    .btn-sm {
      padding: 0.5rem 1rem;
      font-size: 0.8rem;
    }

    /* Quick Actions */
    .quick-actions {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 1rem;
      margin-top: 1.5rem;
    }

    .action-btn {
      background: rgba(255, 255, 255, 0.9);
      border: 2px solid #e2e8f0;
      border-radius: 15px;
      padding: 1.5rem 1rem;
      text-align: center;
      text-decoration: none;
      color: #4a5568;
      transition: all 0.3s ease;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.5rem;
    }

    .action-btn:hover {
      background: white;
      border-color: #4299e1;
      transform: translateY(-3px);
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    }

    .action-icon {
      font-size: 2rem;
      margin-bottom: 0.5rem;
    }

    .action-label {
      font-weight: 600;
      font-size: 0.9rem;
    }

    /* Activity List */
    .activity-list {
      list-style: none;
      max-height: 300px;
      overflow-y: auto;
    }

    .activity-item {
      padding: 1rem;
      border-bottom: 1px solid #e2e8f0;
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .activity-item:last-child {
      border-bottom: none;
    }

    .activity-icon {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.2rem;
    }

    .activity-content {
      flex: 1;
    }

    .activity-title {
      font-weight: 600;
      color: #2d3748;
      margin-bottom: 0.25rem;
    }

    .activity-time {
      font-size: 0.85rem;
      color: #718096;
    }

    /* System Status */
    .status-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
      gap: 1rem;
      margin-top: 1rem;
    }

    .status-item {
      text-align: center;
      padding: 1rem;
      background: rgba(255, 255, 255, 0.8);
      border-radius: 10px;
      border: 2px solid transparent;
    }

    .status-item.online {
      border-color: #38a169;
    }

    .status-item.offline {
      border-color: #e53e3e;
    }

    .status-item.warning {
      border-color: #ed8936;
    }

    .status-label {
      font-size: 0.8rem;
      color: #718096;
      margin-bottom: 0.5rem;
    }

    .status-value {
      font-weight: 700;
      font-size: 1.1rem;
    }

    /* Charts Section */
    .charts-section {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
      gap: 2rem;
      margin-top: 2rem;
    }

    .chart-container {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      border-radius: 20px;
      padding: 2rem;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.5);
    }

    .chart-placeholder {
      height: 200px;
      background: linear-gradient(135deg, #f7fafc, #edf2f7);
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #718096;
      font-weight: 600;
    }

    /* Footer Styles */
    footer {
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(10px);
      color: rgba(255, 255, 255, 0.9);
      text-align: center;
      padding: 2rem;
      margin-top: 3rem;
      border-top: 1px solid rgba(255, 255, 255, 0.2);
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .header-content {
        flex-direction: column;
        gap: 1rem;
      }

      .nav-links {
        flex-wrap: wrap;
        justify-content: center;
      }

      .dashboard-grid {
        grid-template-columns: 1fr;
      }

      .charts-section {
        grid-template-columns: 1fr;
      }
      
      h2 {
        font-size: 2rem;
      }
    }

    /* Floating Elements */
    .floating-elements {
      position: fixed;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      pointer-events: none;
      z-index: -1;
    }

    .floating-element {
      position: absolute;
      font-size: 2rem;
      opacity: 0.1;
      animation: floatAround 20s linear infinite;
    }

    @keyframes floatAround {
      0% { transform: translate(0, 0) rotate(0deg); }
      25% { transform: translate(100px, 50px) rotate(90deg); }
      50% { transform: translate(50px, 100px) rotate(180deg); }
      75% { transform: translate(-50px, 50px) rotate(270deg); }
      100% { transform: translate(0, 0) rotate(360deg); }
    }

    /* No Data State */
    .no-data {
      text-align: center;
      padding: 3rem;
      color: #718096;
      font-style: italic;
    }

    /* Progress Bars */
    .progress-bar {
      width: 100%;
      height: 8px;
      background: #e2e8f0;
      border-radius: 4px;
      overflow: hidden;
      margin-top: 0.5rem;
    }

    .progress-fill {
      height: 100%;
      background: linear-gradient(135deg, #6a11cb, #2575fc);
      border-radius: 4px;
      transition: width 0.3s ease;
    }

    /* Recent Activity Colors */
    .activity-new { background: rgba(72, 187, 120, 0.1); }
    .activity-update { background: rgba(66, 153, 225, 0.1); }
    .activity-warning { background: rgba(237, 137, 54, 0.1); }
    .activity-error { background: rgba(229, 62, 62, 0.1); }
  </style>
</head>
<body>
  <!-- Floating Elements -->
  <div class="floating-elements">
    <div class="floating-element" style="top: 10%; left: 5%;">⚙️</div>
    <div class="floating-element" style="top: 20%; right: 10%;">📊</div>
    <div class="floating-element" style="bottom: 30%; left: 15%;">🔧</div>
    <div class="floating-element" style="bottom: 20%; right: 5%;">👑</div>
  </div>

  <!-- Header -->
  <header>
    <div class="header-content">
      <div class="logo">Admin Dashboard</div>
      <nav class="nav-links">
        <a href="/dashboard">Dashboard</a>
        <a href="/customers">Customers</a>
        <a href="/vehicles">Vehicles</a>
        <a href="/sessions">Sessions</a>
        <a href="/reservations">Reservations</a>
        <a href="/admin" class="active">Admin</a>
      </nav>
    </div>
  </header>

  <main class="container">
    <h2>System Administration</h2>

    <!-- Alert Messages -->
    <% if (typeof success !== 'undefined') { %>
      <div class="alert alert-success">
        <% if (success === 'system_updated') { %>
          ✅ System settings updated successfully!
        <% } else if (success === 'backup_created') { %>
          ✅ System backup created successfully!
        <% } else if (success === 'cache_cleared') { %>
          ✅ System cache cleared successfully!
        <% } %>
      </div>
    <% } %>

    <% if (typeof error !== 'undefined') { %>
      <div class="alert alert-error">
        <% if (error === 'operation_failed') { %>
          ❌ Operation failed. Please try again.
        <% } else if (error === 'access_denied') { %>
          ❌ Access denied. Insufficient permissions.
        <% } else { %>
          ❌ An error occurred. Please try again.
        <% } %>
      </div>
    <% } %>

    <!-- Main Stats Section -->
    <div class="stats-section">
      <div class="stats-grid">
        <div class="stat-item" onclick="window.location.href='/customers'">
          <div class="stat-number"><%= stats.totalCustomers %></div>
          <div class="stat-label">Total Customers</div>
          <div class="progress-bar">
            <div class="progress-fill" style="width: <%= Math.min(stats.totalCustomers / 100 * 100, 100) %>%"></div>
          </div>
        </div>

        <div class="stat-item" onclick="window.location.href='/sessions'">
          <div class="stat-number"><%= stats.todaySessions %></div>
          <div class="stat-label">Today's Sessions</div>
          <div class="progress-bar">
            <div class="progress-fill" style="width: <%= Math.min(stats.todaySessions / 50 * 100, 100) %>%"></div>
          </div>
        </div>

        <div class="stat-item" onclick="window.location.href='/sessions'">
          <div class="stat-number">Ksh <%= stats.todayRevenue.toFixed(2) %></div>
          <div class="stat-label">Today's Revenue</div>
          <div class="progress-bar">
            <div class="progress-fill" style="width: <%= Math.min(stats.todayRevenue / 5000 * 100, 100) %>%"></div>
          </div>
        </div>

        <div class="stat-item" onclick="window.location.href='/sessions'">
          <div class="stat-number"><%= stats.activeSessions %></div>
          <div class="stat-label">Active Sessions</div>
          <div class="progress-bar">
            <div class="progress-fill" style="width: <%= Math.min(stats.activeSessions / 20 * 100, 100) %>%"></div>
          </div>
        </div>
      </div>
    </div>

    <!-- Quick Actions -->
    <div class="dashboard-card">
      <div class="card-header">
        <h3 class="card-title">🚀 Quick Actions</h3>
      </div>
      <div class="quick-actions">
        <a href="/customers" class="action-btn">
          <div class="action-icon">👥</div>
          <div class="action-label">Manage Customers</div>
        </a>
        <a href="/vehicles" class="action-btn">
          <div class="action-icon">🚗</div>
          <div class="action-label">Manage Vehicles</div>
        </a>
        <a href="/slots" class="action-btn">
          <div class="action-icon">🅿️</div>
          <div class="action-label">Manage Slots</div>
        </a>
        <a href="/reservations" class="action-btn">
          <div class="action-icon">📅</div>
          <div class="action-label">Manage Reservations</div>
        </a>
        <a href="#" class="action-btn" onclick="showSystemSettings()">
          <div class="action-icon">⚙️</div>
          <div class="action-label">System Settings</div>
        </a>
        <a href="#" class="action-btn" onclick="createBackup()">
          <div class="action-icon">💾</div>
          <div class="action-label">Create Backup</div>
        </a>
      </div>
    </div>

    <!-- Dashboard Grid -->
    <div class="dashboard-grid">
      <!-- System Status -->
      <div class="dashboard-card">
        <div class="card-header">
          <h3 class="card-title">🟢 System Status</h3>
        </div>
        <div class="status-grid">
          <div class="status-item online">
            <div class="status-label">Database</div>
            <div class="status-value">Online</div>
          </div>
          <div class="status-item online">
            <div class="status-label">API Server</div>
            <div class="status-value">Online</div>
          </div>
          <div class="status-item online">
            <div class="status-label">Payment Gateway</div>
            <div class="status-value">Online</div>
          </div>
          <div class="status-item online">
            <div class="status-label">SMS Service</div>
            <div class="status-value">Online</div>
          </div>
        </div>
        <div style="margin-top: 1rem; text-align: center;">
          <div class="status-label">System Uptime</div>
          <div class="stat-number" style="font-size: 1.5rem;">99.9%</div>
        </div>
      </div>

      <!-- Recent Activity -->
      <div class="dashboard-card">
        <div class="card-header">
          <h3 class="card-title">📋 Recent Activity</h3>
          <div class="card-actions">
            <button class="btn btn-primary btn-sm" onclick="refreshActivities()">
              <span>🔄</span>
              Refresh
            </button>
          </div>
        </div>
        <ul class="activity-list">
          <li class="activity-item activity-new">
            <div class="activity-icon">👤</div>
            <div class="activity-content">
              <div class="activity-title">New customer registered</div>
              <div class="activity-time">2 minutes ago</div>
            </div>
          </li>
          <li class="activity-item activity-update">
            <div class="activity-icon">🚗</div>
            <div class="activity-content">
              <div class="activity-title">Parking session started</div>
              <div class="activity-time">5 minutes ago</div>
            </div>
          </li>
          <li class="activity-item activity-warning">
            <div class="activity-icon">⚠️</div>
            <div class="activity-content">
              <div class="activity-title">Slot A-05 occupancy extended</div>
              <div class="activity-time">15 minutes ago</div>
            </div>
          </li>
          <li class="activity-item activity-update">
            <div class="activity-icon">💳</div>
            <div class="activity-content">
              <div class="activity-title">Payment processed - Ksh 500</div>
              <div class="activity-time">30 minutes ago</div>
            </div>
          </li>
          <li class="activity-item activity-new">
            <div class="activity-icon">📅</div>
            <div class="activity-content">
              <div class="activity-title">New reservation created</div>
              <div class="activity-time">1 hour ago</div>
            </div>
          </li>
        </ul>
      </div>

      <!-- System Information -->
      <div class="dashboard-card">
        <div class="card-header">
          <h3 class="card-title">ℹ️ System Info</h3>
        </div>
        <div style="display: grid; gap: 1rem;">
          <div style="display: flex; justify-content: space-between;">
            <span style="color: #718096;">Server Version:</span>
            <span style="font-weight: 600;">v2.1.0</span>
          </div>
          <div style="display: flex; justify-content: space-between;">
            <span style="color: #718096;">Last Backup:</span>
            <span style="font-weight: 600;">Today, 02:00 AM</span>
          </div>
          <div style="display: flex; justify-content: space-between;">
            <span style="color: #718096;">Database Size:</span>
            <span style="font-weight: 600;">45.2 MB</span>
          </div>
          <div style="display: flex; justify-content: space-between;">
            <span style="color: #718096;">Active Users:</span>
            <span style="font-weight: 600;">12</span>
          </div>
          <div style="display: flex; justify-content: space-between;">
            <span style="color: #718096;">System Load:</span>
            <span style="font-weight: 600; color: #38a169;">Low</span>
          </div>
        </div>
        <div style="margin-top: 1.5rem; padding-top: 1rem; border-top: 1px solid #e2e8f0;">
          <button class="btn btn-primary" onclick="showSystemInfo()" style="width: 100%;">
            <span>📈</span>
            View Detailed Report
          </button>
        </div>
      </div>

      <!-- Maintenance Tools -->
      <div class="dashboard-card">
        <div class="card-header">
          <h3 class="card-title">🔧 Maintenance</h3>
        </div>
        <div style="display: grid; gap: 1rem;">
          <button class="btn" onclick="clearSystemCache()" 
                  style="background: rgba(66, 153, 225, 0.1); color: #3182ce;">
            <span>🗑️</span>
            Clear System Cache
          </button>
          <button class="btn" onclick="optimizeDatabase()"
                  style="background: rgba(72, 187, 120, 0.1); color: #38a169;">
            <span>⚡</span>
            Optimize Database
          </button>
          <button class="btn" onclick="runSystemDiagnostics()"
                  style="background: rgba(237, 137, 54, 0.1); color: #ed8936;">
            <span>🔍</span>
            Run Diagnostics
          </button>
          <button class="btn" onclick="showLogs()"
                  style="background: rgba(159, 122, 234, 0.1); color: #9f7aea;">
            <span>📋</span>
            View System Logs
          </button>
        </div>
      </div>
    </div>

    <!-- Charts Section -->
    <div class="charts-section">
      <div class="chart-container">
        <div class="card-header">
          <h3 class="card-title">📈 Revenue Trends</h3>
        </div>
        <div class="chart-placeholder">
          Revenue Chart - Last 7 Days
        </div>
      </div>

      <div class="chart-container">
        <div class="card-header">
          <h3 class="card-title">🚗 Parking Utilization</h3>
        </div>
        <div class="chart-placeholder">
          Utilization Chart - Today
        </div>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer>
    <p>&copy; 2023 Parking Management System. All rights reserved.</p>
    <p style="margin-top: 0.5rem; font-size: 0.9rem;">
      Logged in as: <%= user.username %> (Administrator) 
      | Server Time: <span id="serverTime"><%= new Date().toLocaleString() %></span>
      | <a href="/logout" style="color: rgba(255,255,255,0.8);">Logout</a>
    </p>
  </footer>

  <script>
    // Add interactive elements
    document.addEventListener('DOMContentLoaded', function() {
      const buttons = document.querySelectorAll('.btn');
      buttons.forEach(button => {
        button.addEventListener('mouseenter', function() {
          this.style.transform = 'translateY(-2px)';
        });
        
        button.addEventListener('mouseleave', function() {
          this.style.transform = 'translateY(0)';
        });
      });

      // Add fade-in animation for cards
      const cards = document.querySelectorAll('.dashboard-card');
      cards.forEach((card, index) => {
        card.style.opacity = '0';
        card.style.transform = 'translateY(20px)';
        card.style.transition = 'all 0.5s ease';
        
        setTimeout(() => {
          card.style.opacity = '1';
          card.style.transform = 'translateY(0)';
        }, 100 * index);
      });

      // Auto-hide alerts after 5 seconds
      const alerts = document.querySelectorAll('.alert');
      alerts.forEach(alert => {
        setTimeout(() => {
          alert.style.opacity = '0';
          alert.style.transition = 'opacity 0.5s ease';
          setTimeout(() => alert.remove(), 500);
        }, 5000);
      });

      // Update server time
      function updateServerTime() {
        const now = new Date();
        document.getElementById('serverTime').textContent = now.toLocaleString();
      }
      setInterval(updateServerTime, 1000);
    });

    // Admin functions
    function showSystemSettings() {
      alert('System Settings panel would open here.\n\nFeatures:\n• Parking Rates Configuration\n• Operating Hours\n• Notification Settings\n• Security Settings\n• Payment Gateway Configuration');
    }

    function createBackup() {
      if (confirm('Create a system backup?\nThis may take a few minutes.')) {
        // Simulate backup process
        const btn = event.target.closest('.action-btn');
        const originalHtml = btn.innerHTML;
        btn.innerHTML = '<div class="action-icon">⏳</div><div class="action-label">Creating Backup...</div>';
        btn.style.pointerEvents = 'none';
        
        setTimeout(() => {
          btn.innerHTML = originalHtml;
          btn.style.pointerEvents = 'auto';
          alert('✅ System backup created successfully!\nBackup file: system_backup_' + new Date().toISOString().split('T')[0] + '.sql');
        }, 2000);
      }
    }

    function clearSystemCache() {
      if (confirm('Clear system cache?\nThis will improve performance.')) {
        // Simulate cache clearing
        event.target.innerHTML = '<span>⏳</span> Clearing...';
        event.target.disabled = true;
        
        setTimeout(() => {
          event.target.innerHTML = '<span>✅</span> Cache Cleared';
          setTimeout(() => {
            event.target.innerHTML = '<span>🗑️</span> Clear System Cache';
            event.target.disabled = false;
          }, 2000);
        }, 1500);
      }
    }

    function optimizeDatabase() {
      if (confirm('Optimize database?\nThis will improve query performance.')) {
        event.target.innerHTML = '<span>⏳</span> Optimizing...';
        event.target.disabled = true;
        
        setTimeout(() => {
          event.target.innerHTML = '<span>✅</span> Optimized';
          setTimeout(() => {
            event.target.innerHTML = '<span>⚡</span> Optimize Database';
            event.target.disabled = false;
          }, 2000);
        }, 2000);
      }
    }

    function runSystemDiagnostics() {
      alert('Running system diagnostics...\n\n✅ Database Connection: OK\n✅ File System: OK\n✅ Payment Gateway: OK\n✅ Email Service: OK\n✅ SMS Gateway: OK\n\nAll systems operational.');
    }

    function showLogs() {
      alert('System logs would open here.\n\nRecent log entries:\n• INFO: System started at 06:00:00\n• INFO: Backup completed at 02:00:00\n• WARN: High memory usage at 14:30:00\n• INFO: Cache cleared at 15:45:00');
    }

    function showSystemInfo() {
      alert('Detailed System Report:\n\nServer: Ubuntu 20.04 LTS\nPHP Version: 8.1.12\nDatabase: MySQL 8.0.32\nMemory Usage: 45%\nCPU Usage: 23%\nDisk Usage: 62%\nUptime: 15 days, 6 hours');
    }

    function refreshActivities() {
      const activityList = document.querySelector('.activity-list');
      activityList.style.opacity = '0.5';
      
      setTimeout(() => {
        activityList.style.opacity = '1';
        alert('Activities refreshed!');
      }, 1000);
    }

    // Real-time updates simulation
    function simulateRealTimeUpdates() {
      // In a real application, this would use WebSockets
      setInterval(() => {
        // Update random stat for demo purposes
        const randomStat = document.querySelector('.stat-item:nth-child(2) .stat-number');
        if (randomStat) {
          const current = parseInt(randomStat.textContent);
          randomStat.textContent = current + Math.floor(Math.random() * 3);
        }
      }, 10000);
    }

    simulateRealTimeUpdates();
  </script>
</body>
</html>