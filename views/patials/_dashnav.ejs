<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Parking Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #4361ee;
            --secondary-color: #3f37c9;
            --light-color: #f8f9fa;
            --dark-color: #212529;
            --success-color: #4cc9f0;
            --warning-color: #f72585;
            --sidebar-width: 250px;
            --transition-speed: 0.3s;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f5f7fb;
            overflow-x: hidden;
        }
        
        .sidebar {
            background: linear-gradient(180deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            color: white;
            height: 100vh;
            position: fixed;
            top: 90px;
            left: 0;
            width: var(--sidebar-width);
            transition: all var(--transition-speed) ease;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            z-index: 1000;
            border-radius: 5px;
            /* overflow-y: scroll; */
        }
        
        .sidebar.collapsed {
            width: 70px;
        }
        
        .sidebar-header {
            padding: 20px 15px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        
        .sidebar-header h3 {
            font-size: 1.5rem;
            margin: 0;
            font-weight: 600;
            white-space: nowrap;
            overflow: hidden;
        }
        
        .sidebar.collapsed .sidebar-header h3 {
            display: none;
        }
        
        .toggle-btn {
            background: rgba(255, 255, 255, 0.1);
            border: none;
            color: white;
            border-radius: 50%;
            width: 50px;
            height: 35px;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
            
        }
        
        .toggle-btn:hover {
            background: rgba(255, 255, 255, 0.2);
        }
        
        .sidebar.collapsed .toggle-btn {
            transform: rotate(180deg);
        }
        
        .sidebar-menu {
            padding: 15px 0;
            height: calc(100vh - 80px);
            overflow-y: auto;
        }
        
        .nav-item {
            margin-bottom: 5px;
        }
        
        .nav-link {
            color: rgba(255, 255, 255, 0.8);
            padding: 12px 20px;
            border-radius: 0;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            white-space: nowrap;
            overflow: hidden;
        }
        
        .nav-link:hover {
            color: white;
            background-color: rgba(255, 255, 255, 0.1);
        }
        
        .nav-link.active {
            color: white;
            background-color: rgba(255, 255, 255, 0.2);
            border-left: 4px solid white;
            font-weight: 500;
        }
        
        .nav-link i {
            width: 25px;
            text-align: center;
            margin-right: 10px;
            font-size: 1.1rem;
        }
        
        .sidebar.collapsed .nav-link span {
            display: none;
        }
        
        .sidebar.collapsed .nav-link i {
            margin-right: 0;
        }
        
        .sidebar-heading {
            color: rgba(255, 255, 255, 0.6);
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            padding: 20px 20px 10px;
            margin-top: 10px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .sidebar.collapsed .sidebar-heading {
            display: none;
        }
        
        .user-info {
            padding: 15px 20px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            display: flex;
            align-items: center;
        }
        
        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background-color: rgba(255, 255, 255, 0.2);
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 10px;
            font-size: 1.2rem;
        }
        
        .user-details {
            flex-grow: 1;
            white-space: nowrap;
            overflow: hidden;
        }
        
        .user-name {
            font-weight: 500;
            margin-bottom: 2px;
        }
        
        .user-role {
            font-size: 0.8rem;
            color: rgba(255, 255, 255, 0.7);
        }
        
        .sidebar.collapsed .user-details {
            display: none;
        }
        
        .main-content {
            margin-left: var(--sidebar-width);
            padding: 20px;
            transition: all var(--transition-speed) ease;
        }
        
        .sidebar.collapsed ~ .main-content {
            margin-left: 70px;
        }
        
        .page-header {
            background: white;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            margin-bottom: 20px;
        }
        
        .stats-card {
            background: white;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            margin-bottom: 20px;
            transition: transform 0.3s ease;
        }
        
        .stats-card:hover {
            transform: translateY(-5px);
        }
        
        .stats-icon {
            width: 60px;
            height: 60px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            margin-bottom: 15px;
        }
        
        .stats-icon.primary {
            background-color: rgba(67, 97, 238, 0.1);
            color: var(--primary-color);
        }
        
        .stats-icon.success {
            background-color: rgba(76, 201, 240, 0.1);
            color: var(--success-color);
        }
        
        .stats-icon.warning {
            background-color: rgba(247, 37, 133, 0.1);
            color: var(--warning-color);
        }
        
        .stats-number {
            font-size: 1.8rem;
            font-weight: 600;
            margin-bottom: 5px;
        }
        
        .stats-label {
            color: #6c757d;
            font-size: 0.9rem;
        }
        
        /* Custom scrollbar for sidebar */
        .sidebar-menu::-webkit-scrollbar {
            width: 5px;
        }
        
        .sidebar-menu::-webkit-scrollbar-track {
            background: rgba(255, 255, 255, 0.1);
        }
        
        .sidebar-menu::-webkit-scrollbar-thumb {
            background: rgba(255, 255, 255, 0.3);
            border-radius: 10px;
        }
        
        .sidebar-menu::-webkit-scrollbar-thumb:hover {
            background: rgba(255, 255, 255, 0.5);
        }
        
        /* Responsive adjustments */
        @media (max-width: 768px) {
            .sidebar {
                width: 70px;
            }
            
            .sidebar-header h3, 
            .nav-link span, 
            .sidebar-heading,
            .user-details {
                display: none;
            }
            
            .main-content {
                margin-left: 70px;
            }
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <nav class="sidebar" id="sidebarMenu">
        <div class="sidebar-header">
            <h3><i class="fas fa-parking me-2"></i> ParkSmart</h3>
            <button class="toggle-btn" id="sidebarToggle">
                <i class="fas fa-chevron-left"></i>
            </button>
        </div>
        
       <div class="position-sticky pt-3">
        <div class="sidebar-sticky">
            <ul class="nav flex-column">
                <li class="nav-item">
                    <a class="nav-link <%= currentPage === 'dashboard' ? 'active' : '' %>" 
                       href="<%= isAdmin || isOperator ? '/admin/dashboard' : '/user/dashboard' %>">
                        <i class="fas fa-tachometer-alt me-2"></i>
                        Dashboard
                    </a>
                </li>
                
                <% if (isAdmin || isOperator) { %>
                    <!-- Admin/Operator Menu -->
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'sessions' ? 'active' : '' %>" href="/admin/sessions">
                            <i class="fas fa-clock me-2"></i>
                            Parking Sessions
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'reservations' ? 'active' : '' %>" href="/admin/reservations">
                            <i class="fas fa-calendar-check me-2"></i>
                            Reservations
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'vehicles' ? 'active' : '' %>" href="/admin/vehicles">
                            <i class="fas fa-car me-2"></i>
                            Vehicles
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'slots' ? 'active' : '' %>" href="/admin/slots">
                            <i class="fas fa-parking me-2"></i>
                            Parking Slots
                        </a>
                    </li>


                    

                    <% if (isAdmin) { %>
                        <li class="nav-item">
                            <a class="nav-link <%= currentPage === 'customers' ? 'active' : '' %>" href="/admin/customers">
                                <i class="fas fa-users me-2"></i>
                                Customers
                            </a>
                        </li>
                    <% } %>
                <% } else { %>
                    <!-- Customer Menu -->
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'reservations' ? 'active' : '' %>" href="/user/reservations">
                            <i class="fas fa-calendar-check me-2"></i>
                            My Reservations
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'vehicles' ? 'active' : '' %>" href="/user/vehicles">
                            <i class="fas fa-car me-2"></i>
                            My Vehicles
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'sessions' ? 'active' : '' %>" href="/user/sessions">
                            <i class="fas fa-clock me-2"></i>
                            My Sessions
                        </a>
                    </li>
                <% } %>
                
                <li class="nav-item">
                    <a class="nav-link <%= currentPage === 'profile' ? 'active' : '' %>" href="/user/profile">
                        <i class="fas fa-user me-2"></i>
                        Profile
                    </a>
                </li>

              

            <% if (isAdmin || isOperator) { %>
                <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
                    <span>Administration</span>
                </h6>
                <ul class="nav flex-column mb-2">
                    <li class="nav-item">
                        <span class="nav-link text-muted">
                            <i class="fas fa-user-shield me-2"></i>
                            Role: <%= userRole %>
                        </span>
                    </li>
                </ul>
            <% } %>
        </div>
    </div>
                  <li class="nav-item">
            <a class="nav-link text-danger" href="/logout">
              <i class="fas fa-sign-out-alt me-1"></i> Logout
            </a>
          </li>
        <div class="user-info">
            <div class="user-avatar">
                <i class="fas fa-user"></i>
            </div>
            <div class="user-details">
             <span>Role: <%= user.role_name %></span>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
   

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        // Toggle sidebar
        document.getElementById('sidebarToggle').addEventListener('click', function() {
            document.getElementById('sidebarMenu').classList.toggle('collapsed');
        });
        
        // Initialize charts
        const activityCtx = document.getElementById('activityChart').getContext('2d');
        const activityChart = new Chart(activityCtx, {
            type: 'line',
            data: {
                labels: ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'],
                datasets: [{
                    label: 'Parking Sessions',
                    data: [120, 150, 180, 130, 170, 90, 110],
                    borderColor: '#4361ee',
                    backgroundColor: 'rgba(67, 97, 238, 0.1)',
                    tension: 0.4,
                    fill: true
                }, {
                    label: 'Reservations',
                    data: [80, 100, 120, 90, 110, 70, 85],
                    borderColor: '#4cc9f0',
                    backgroundColor: 'rgba(76, 201, 240, 0.1)',
                    tension: 0.4,
                    fill: true
                }]
            },
            options: {
                responsive: true,
                plugins: {
                    legend: {
                        position: 'top',
                    }
                }
            }
        });
        
        const occupancyCtx = document.getElementById('occupancyChart').getContext('2d');
        const occupancyChart = new Chart(occupancyCtx, {
            type: 'doughnut',
            data: {
                labels: ['Occupied', 'Available', 'Reserved'],
                datasets: [{
                    data: [65, 24, 11],
                    backgroundColor: [
                        '#4361ee',
                        '#4cc9f0',
                        '#f72585'
                    ],
                    borderWidth: 0
                }]
            },
            options: {
                responsive: true,
                plugins: {
                    legend: {
                        position: 'bottom',
                    }
                }
            }
        });
        
        // Simulate user role for demonstration
        // In a real application, this would come from your backend
        const userRole = 'admin'; // Can be 'admin', 'operator', or 'customer'
        
        // Update sidebar based on user role
        function updateSidebarForUser(role) {
            const sidebarMenu = document.querySelector('.sidebar-menu');
            
            // Clear existing menu items except dashboard and profile
            const existingItems = sidebarMenu.querySelectorAll('.nav-item');
            existingItems.forEach(item => {
                if (!item.querySelector('a[href*="dashboard"]') && !item.querySelector('a[href*="profile"]')) {
                    item.remove();
                }
            });
            
            // Get the dashboard item to insert after
            const dashboardItem = sidebarMenu.querySelector('a[href*="dashboard"]').closest('.nav-item');
            
            if (role === 'admin' || role === 'operator') {
                // Admin/Operator Menu
                const adminMenuItems = [
                    { href: '/admin/sessions', icon: 'fas fa-clock', text: 'Parking Sessions' },
                    { href: '/admin/reservations', icon: 'fas fa-calendar-check', text: 'Reservations' },
                    { href: '/admin/vehicles', icon: 'fas fa-car', text: 'Vehicles' },
                    { href: '/admin/slots', icon: 'fas fa-parking', text: 'Parking Slots' }
                ];
                
                if (role === 'admin') {
                    adminMenuItems.push({ href: '/admin/customers', icon: 'fas fa-users', text: 'Customers' });
                }
                
                // Insert admin menu items after dashboard
                adminMenuItems.forEach(item => {
                    const li = document.createElement('li');
                    li.className = 'nav-item';
                    li.innerHTML = `
                        <a class="nav-link" href="${item.href}">
                            <i class="${item.icon}"></i>
                            <span>${item.text}</span>
                        </a>
                    `;
                    dashboardItem.after(li);
                });
                
                // Add administration section
                const adminSection = document.createElement('h6');
                adminSection.className = 'sidebar-heading';
                adminSection.textContent = 'Administration';
                
                const adminRoleItem = document.createElement('ul');
                adminRoleItem.className = 'nav flex-column mb-2';
                adminRoleItem.innerHTML = `
                    <li class="nav-item">
                        <span class="nav-link">
                            <i class="fas fa-user-shield"></i>
                            <span>Role: ${role.charAt(0).toUpperCase() + role.slice(1)}</span>
                        </span>
                    </li>
                `;
                
                sidebarMenu.appendChild(adminSection);
                sidebarMenu.appendChild(adminRoleItem);
                
            } else {
                // Customer Menu
                const customerMenuItems = [
                    { href: '/user/reservations', icon: 'fas fa-calendar-check', text: 'My Reservations' },
                    { href: '/user/vehicles', icon: 'fas fa-car', text: 'My Vehicles' },
                    { href: '/user/sessions', icon: 'fas fa-clock', text: 'My Sessions' }
                ];
                
                // Insert customer menu items after dashboard
                customerMenuItems.forEach(item => {
                    const li = document.createElement('li');
                    li.className = 'nav-item';
                    li.innerHTML = `
                        <a class="nav-link" href="${item.href}">
                            <i class="${item.icon}"></i>
                            <span>${item.text}</span>
                        </a>
                    `;
                    dashboardItem.after(li);
                });
            }
            
            // Update user info based on role
            const userInfo = document.querySelector('.user-info');
            const userName = userInfo.querySelector('.user-name');
            const userRoleText = userInfo.querySelector('.user-role');
            
            if (role === 'admin') {
                userName.textContent = 'John Doe';
                userRoleText.textContent = 'Administrator';
            } else if (role === 'operator') {
                userName.textContent = 'Jane Smith';
                userRoleText.textContent = 'Operator';
            } else {
                userName.textContent = 'Michael Johnson';
                userRoleText.textContent = 'Customer';
            }
        }
        
        // Initialize sidebar based on user role
        updateSidebarForUser(userRole);
    </script>
</body>
</html>